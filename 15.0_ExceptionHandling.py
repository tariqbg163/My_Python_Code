# Exception Handling

# Python by default raise an exception object at exception line and handled by 
# python default except mechanism
# default try and default except mechanism

a=eval(input("enter a number >> "))
b=eval(input("enter a number >> "))
print(a/b)  # predefined exception(try and except will execute which crash the program
 # it crash the program execution from user use
 
print("is Exception is occured") # this line will not execute when exception occur
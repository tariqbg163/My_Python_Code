from sys import argv  # sys is predefined module and argv (argument vector) is a variable
print(argv) # argv is type of list which contain fileName at index 0

# this program should be run on commond line